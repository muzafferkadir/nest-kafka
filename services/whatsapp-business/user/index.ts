// import request from '../../../utils/request';
// import { AckIdentityRequestBody, CheckAsyncRequestBody, CreateRequestBody } from './user.types';

// export const create = (data: CreateRequestBody) => request.post<any>('/v1/contacts', data);

// export const check = (data: CheckAsyncRequestBody) => request.post<any>('/v1/contacts', data);

// export const getIdentity = () => request.get<any>('/v1/contacts/{{Recipient-WA-ID}}/identity');

// export const ackIdentity = (data: AckIdentityRequestBody) =>
//   request.put('/v1/contacts/{{Recipient-WA-ID}}/identity', data);
